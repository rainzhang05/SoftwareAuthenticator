[package]
name = "transport-core"
version = "0.1.0"
edition = "2021"

[dependencies]
rand_core = { version = "0.6", features = ["getrandom"] }
p256 = { version = "0.13", features = ["pkcs8"] }
rcgen = "0.11"
